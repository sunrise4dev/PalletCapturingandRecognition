from setuptools import setup, find_packages

setup(
    name="pallet_recognition",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.9",
    description="Pallet Capturing and Recognition ML Pipeline",
)
